import type { Meta, StoryObj } from '@storybook/react';
import FleetMonitoringTimeplan from '../components/contents/FleetMonitoringTimeplan';

const meta: Meta<typeof FleetMonitoringTimeplan> = {
  component: FleetMonitoringTimeplan,
};

export default meta;
type Story = StoryObj<typeof FleetMonitoringTimeplan>;

export const Example: Story = {
  args: {
    
  },
};